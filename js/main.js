const { createApp } = Vue

const options = {
    template: `
`,
    data() {
        return {};
    },
    created() { },
    methods: {},
    computed: {},
    unmounted() { },
};

const app = createApp(options)
app.mount('#app')